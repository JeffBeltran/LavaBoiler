import Vue from 'vue';

export const Login = Vue.component('login', require('./Login.vue'));
export const Register = Vue.component('register', require('./Register.vue'));
export const Email = Vue.component('email', require('./Email.vue'));
